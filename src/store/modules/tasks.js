const state = {
    currentTaskId: 4,
};

const getters = {
    currentTaskId: state => {
        return state.currentTaskId;
    },

};

const mutations = {
    
};

const actions = {

};

export default {
    state,
    getters,
    mutations,
    actions
}